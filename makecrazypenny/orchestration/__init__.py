"""Layer 2: orchestration built on the Claude Agent SDK.

Defines the specialist sub-agents and the mother-orchestrator options
(``agents.py``) and the CLI entrypoint (``main.py``). All SDK imports are
guarded so this package imports without the SDK installed.
"""
